<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
</head>
<style>
  body {
    font-family: 'Arial', sans-serif;
    background-color: #f3f4f6;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100vh;
    color: #333;
}

h1 {
    font-size: 1.8rem;
    color: #2c3e50;
    text-align: center;
    margin-bottom: 20px;
}

form {
    background: #ffffff;
    border-radius: 8px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
    padding: 20px 40px;
    max-width: 400px;
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 15px;
    align-items: center;
}

form input {
    width: 100%;
    padding: 10px;
    border-radius: 5px;
    border: 1px solid #dcdcdc;
    font-size: 1rem;
    transition: 0.3s;
}

form input:focus {
    border-color: #3498db;
    outline: none;
    box-shadow: 0 0 8px rgba(52, 152, 219, 0.4);
}

form button {
    background-color: #3498db;
    color: white;
    padding: 10px 20px;
    font-size: 1rem;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

form button:hover {
    background-color: #2980b9;
}

</style>
<h1>Edit Task</h1>
<form action="/updateTask" method="post">
    <input type="text" value="<%= singleTask.id %>" name="id" hidden>
    <input type="text" value="<%= singleTask.task %>" placeholder="Enter Task" name="task">
    <input type="datetime-local" value="<%= singleTask.startTime %>" name="startTime" required>
    <input type="datetime-local" value="<%= singleTask.endTime %>" name="endTime" required>
    <button type="submit">Update</button>
</form>
</body>
</html>
